apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubernetes-apache-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "kubernetes-apache-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubernetes-apache-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

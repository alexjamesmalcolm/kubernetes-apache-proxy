apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubernetes-apache-proxy.fullname" . }}
  labels:
    {{- include "kubernetes-apache-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubernetes-apache-proxy.selectorLabels" . | nindent 4 }}
